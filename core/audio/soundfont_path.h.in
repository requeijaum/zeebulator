#pragma once

// Generated by CMake (see the top-level CMakeLists.txt's
// generaluser_gs_soundfont FetchContent declaration) -- not committed,
// regenerated on every configure. The path itself is real (points at
// the actual fetched .sf2 on this machine), so consumers can just
// tsf_load_filename() it directly.
namespace zeebulator {
constexpr const char* kBundledSoundFontPath = "@generaluser_gs_soundfont_SOURCE_DIR@/GeneralUser-GS.sf2";
}  // namespace zeebulator
